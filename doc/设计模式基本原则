几大原则

1.单一职责原则
    每个类只做自己分内的事情，有新的责任，应该加新的类
2.开闭原则
    对修改关闭，尽量少的用public。对扩展开放
3.里氏替换原则
    使用基类的情况下，都能使用子类。
4.依赖倒置
    高层次实现不依赖底层次实现，所有依赖都是对抽象的。
5.接口隔离原则
    避免使用方使用不需要的接口，所有要对大接口进行拆分。也是单一职责的一种体现吧
    

